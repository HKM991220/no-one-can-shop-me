/**
 * 资源 Bundle 名称
 */
export enum BundleName {
    RESOURCES = 'resources',
}

/**
 * UI 页面唯一标识（用于 UIManager 注册/打开/关闭）
 */
export enum UIId {
    LOADING = 'loading',
    GAME = 'game',
}

/**
 * UI 预制体路径（相对 Bundle）
 */
export enum UIPrefabPath {
    LOADING = 'prefab/Loading',
    GAME = 'prefab/Game',
}